const routes = require('./routes/approvalRoutes');
const controller = require('./controllers/approvalController');
const service = require('./services/approvalService');

module.exports = {
    routes,
    controller,
    service
};
